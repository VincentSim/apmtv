<% if @video.errors.any? %>
  // Render new review form with errors
  $('.hello').html("<%= j render 'video_form', video: @video %>");
  console.log('erreur')
<% else %>
  console.log('pas derreur');
  console.log('<%= @video.youtube_id %>');
  $(".hello[data-id=<%=@video.youtube_id%>]").hide();

<% end %>